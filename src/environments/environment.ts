export const environment = {
  production: true,
  // Set this to wherever the server (this repo's `server/`) is actually
  // deployed, including the /api suffix.
  apiUrl: 'https://thoughtfull.lat/api',
};
